{
	"private": true,
	"workspaces": [
		"language-service-plugin",
		"ts-api-extras",
		"vscode-extension"
	],
	"devDependencies": {
		"vitest": "^4.1.2"
	},
	"scripts": {
		"build": "npm run build -w @vscode/ts-plugin-file-path-support && npm run build -w ts-file-path-support",
		"test": "npm run test -w @vscode/ts-plugin-file-path-support"
	}
}